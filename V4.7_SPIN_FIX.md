# V4.7 Spin Trigger Fix

Mobile wheel transport was hardened for iPhone/Safari: the controller emits `SPIN_WHEEL`, supports Pointer Events and a touch-event fallback, and provides a tap fallback. Host compatibility accepts the new message plus legacy spin names.
